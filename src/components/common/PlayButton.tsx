export default function PlayButton() {
  return (
    <button className="align-middle text-center touch-manipulation relative font-bold text-base cursor-pointer duration-[33ms] rounded-full bg-color-accent-primary hover:scale-105" onClick={() => { }}>
    </button>
  );
}